# MultiRAG Demo Notes

Hybrid retrieval combines semantic search with BM25 keyword search. Semantic
search helps with conceptual questions, while BM25 helps exact technical terms.

Graph retrieval connects source nodes, chunk nodes, and concept nodes so a
learner can inspect relationships across their material.

